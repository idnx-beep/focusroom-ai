# Design

## Current Visual Style

FocusRoom AI 的当前视觉语言是沉浸式、暗色、玻璃拟态、场景摄影和安静专注。界面应像一个可进入的自习室，而不是普通表单工具：背景承担氛围，前景控件保持轻量、半透明、低噪声，文字和按钮只服务于进入学习、调整环境、完成复盘这几件事。

## Design Intent

- 保留电影感全屏场景作为第一视觉信号。
- 保留暗色环境，减少学习时的视觉刺激。
- 保留玻璃拟态作为控制层语言，但避免到处堆叠厚重卡片。
- 保持安静、克制、可信的产品气质，不做强营销、不做夸张动效。
- 所有设计优化都必须先保护番茄钟、复盘、分享卡片和本地数据逻辑。

## Color Principles

- 主背景使用接近黑色的深色基底，当前核心底色为 `#05070b`。
- 前景玻璃层使用白色低透明度、细边框和背景模糊，形成轻微层级。
- 主行动色使用浅天蓝，当前接近 `#7dd3fc` / `sky-300`，用于开始学习、保存复盘、复制分享文案等关键动作。
- 白色文字使用透明度区分层级：主要信息接近纯白，说明文字使用 `white/60` 左右，辅助标签更低。
- 不新增随机强调色；如果要增加语义色，应服务于状态表达，例如成功、警告、错误，而不是装饰。
- 场景图已经承担大面积色彩变化，UI 颜色应保持稳定，避免与场景争抢注意力。

## Typography Principles

- 当前字体以 Inter 和系统 sans-serif 为主，适合作为产品工具界面的稳定基线。
- 首页标题可以更有展示感，但最大字号要受控，移动端优先保证换行自然。
- 控制台、面板、弹窗中的标题要比正文清晰一级，但不使用过大的展示字体。
- 倒计时数字使用 `tabular-nums`，保证计时变化时宽度稳定。
- 标签和状态文案可以使用轻微字距，但不要让所有小标题都变成强烈的全大写装饰。
- 正文行高保持舒适，长目标、复盘内容和分享文案必须允许换行，不撑破布局。

## Card Principles

- 卡片只用于承载可操作的模块、复盘内容、分享卡片和学习历史，不为了装饰而新增卡片。
- 外层容器可以更柔和，内部控件半径应略小，形成清晰层级。
- 避免卡片套卡片越来越厚；当内容很多时，优先用间距、分隔、背景透明度区分。
- 场景卡片需要突出缩略图、标题和标签，手机端优先保证点击面积和可读性。
- 学习历史卡片要优先可扫读：日期、时长、目标和分享入口要清楚，不要挤成一行。

## Button Principles

- 主按钮使用浅天蓝实底，承担最重要动作：开始学习、保存复盘、复制分享文案、确认结束。
- 次级按钮使用透明玻璃样式，适合调整、自定义、取消、关闭等操作。
- 所有可点击按钮高度至少约 44px，移动端不缩得过小。
- 按钮文案使用明确动词，例如“开始学习”“保存复盘”“复制分享文案”“继续学习”。
- 图标只作为识别辅助，不能替代关键文字，除非按钮有明确 aria-label。
- Hover 和 active 反馈要轻微，不制造跳动感。

## Dialog Principles

- 弹窗用于真正打断流程的场景：登录 Demo、休息、结束确认、学习复盘和分享卡片。
- 移动端弹窗必须限制最大高度并允许滚动，不能超出屏幕。
- 关闭按钮保持 44px 左右点击面积，并放在用户预期的位置。
- ReviewDialog 应保持复盘感，不变成复杂问卷；输入框要清晰、可读、易点击。
- ShareCard 应像一张可以截图或复制的成果卡，而不是普通表单摘要。
- 结束专注属于高影响操作，必须有确认，避免 Escape 或误触直接结束学习。

## Mobile Principles

- 手机端优先保证“能进入学习、能暂停继续、能结束复盘、能复制分享”四条主路径。
- 首页导航不能横向溢出，主标题不能过度放大，CTA 要保持明显。
- 控制台在手机端采用上下布局，面板宽度跟随屏幕，标签和按钮允许换行。
- 自习室模式中倒计时不能超出屏幕，底部声音控制可以折叠或压缩。
- 学习历史和分享卡片要优先可读，不为了桌面布局强行塞入多列。
- 使用 Tailwind 的 `sm`、`md`、`lg`、`xl` 断点，尽量避免重复 CSS。

## Motion Principles

- 动效应轻微、克制、不打扰专注。
- 背景淡入、面板轻微上移、按钮微小 hover 都可以保留，但不做循环炫技动效。
- 专注模式中动效要更安静，避免影响倒计时阅读和学习状态。
- 必须支持 `prefers-reduced-motion: reduce`，低性能模式下也要减少模糊和动效成本。
- 不使用会造成布局跳动的动画属性；优先使用 opacity 和 transform。

## Accessibility Principles

- 保持可见键盘焦点，不能只依赖 hover 状态。
- 主内容需要可通过 skip link 进入。
- 重要弹窗需要 `role="dialog"`、`aria-modal` 和可理解的标题。
- 颜色透明度调整时要优先检查文字对比度，尤其是说明文字、placeholder 和按钮文字。
- 图标按钮必须提供 aria-label。
- 长文本、中文目标和复盘内容要支持换行，不能横向撑破屏幕。

## Visual Polish Notes

- 后续 polish 优先提升真实产品完成度，而不是重做风格。
- 玻璃拟态要更“少而准”：关键面板保留，重复装饰减少。
- 首页可以增加更明确的产品状态感，例如当前场景、今日目标或轻量 HUD，但不要变成营销页。
- 控制台可以继续优化信息层级，让场景、计划、声音和历史的优先级更清楚。
- 自习室模式可以进一步减少底部控制噪声，让倒计时和目标成为绝对主角。
- 复盘和分享卡片可以提升排版和节奏，让用户更愿意保存或分享。
- 不引入新的 UI 库；优先用现有 Tailwind、Lucide、组件结构和设计语言继续打磨。

